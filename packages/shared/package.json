{
  "name": "@fitgestao/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Tipos, enums e validações compartilhadas do Nexora Fit.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
